/* eslint-disable */
// Jest mock for the generated Prisma Client server entry (`@scholarsome/prisma/server`).
// The generated ESM client (`client.ts`) cannot be compiled by ts-jest under CommonJS
// because it uses `import.meta.url`. Tests never instantiate the real client (PrismaService
// is always mocked), so a minimal stub is sufficient here.

export class PrismaClient {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  [key: string]: any;
}
